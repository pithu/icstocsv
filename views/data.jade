!!! 5
html
  head
    title ics to csv
  body
    | start, end, duration, summary, description <br>
    for event in events
       | #{event.start.toISOString()}, #{event.end.toISOString()}, #{(event.end - event.start)/(60*60*1000)}, "#{event.summary}", "#{event.description}" <br>

